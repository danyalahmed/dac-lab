apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-route
  namespace: argocd
spec:
  parentRefs:
    - name: main
      namespace: gateway-system
  hostnames: 
    - "argocd.technologylab.uk"
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80